<h1>Upload a photo</h1>

<%= form_with(model: @photo, local: true) do |form| %>
  <div class="field">
    <%= form.label "New Photo" %>
    <%= form.file_field :image, required: true %>
  </div>
  <div class="actions">
    <%= form.submit "Post Photo" %>
  </div>
<% end %>
